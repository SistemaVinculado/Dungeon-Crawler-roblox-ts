// This is a server-side script that will run once when the game server starts.
// It's the entry point for your server-side game logic.

// FIX: Replaced `print()` with `console.log()`. `print()` is not a standard function in a Node.js environment.
console.log("Hello from the server! The game is running.");